apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-example
spec:
  action: pod-kill
  gracePeriod: "550ms"
  duration: "30s"
  mode: fixed
  value: "5"
  selector:
    labelSelectors:
      "app.kubernetes.io/component": "tikv"
